[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "f90wrap>=0.2.6",
    "setuptools_scm[toml]>=3.4"
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "f90wrap_pyhmcode/pyhmcode/version.py"